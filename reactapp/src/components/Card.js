import React from 'react';
// import '../assets/style/Card.css'; // Make sure to create this CSS file or adjust the path accordingly

function Card() {
    return (
        <div className="card">
            Magic Card
        </div>
    );
}

export default Card;
